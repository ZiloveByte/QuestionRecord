#include <bits/stdc++.h>
using namespace std;

void solve(){
    int x,y,carry,maxlen;
    string a,b;
    cin>>a>>b;
    reverse(a.begin(),a.end());
    reverse(b.begin(),b.end());
    maxlen=max(a.length(),b.length());
    for(int i=0;i<maxlen;i++){
        
    }
}

int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    solve();
    return 0;
}
#include <bits/stdc++.h>
using namespace std;

vector<unsigned long long> a;

void solve(){
    int n;
    unsigned long long x,mex;
    cin>>n;
    for(int i=0;i<n;i++){
        cin>>x;
        a.push_back(x);
    }
    int len=a.size();
}

int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    solve();
}